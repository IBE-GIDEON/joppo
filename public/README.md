# public/

Static files served from the site root. A file at `public/showcase.png` is
reachable at `/showcase.png`.

Put here: images, favicon, og-image, fonts you host yourself, robots.txt.

Next.js does not create this folder on its own, which is why it was missing.
